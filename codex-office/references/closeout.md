# Closeout

After reviewer approval: run/verify the full project gate, inspect the final
diff and status, commit only intended files, then push and create a PR only if
the caller authorized those actions. Never merge or deploy without separate
authority. Report the plan path, executor commit range, reviewer model and
round count, full gate result, PR URL if created, and unresolved items.

