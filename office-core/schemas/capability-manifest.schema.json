{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "urn:office-core:1.2.0:capability-manifest.schema.json",
  "title": "Capability manifest",
  "description": "The skills actually selected for a packet. Replaces copying the available-skills catalog or the bodies of task skills into a brief.",
  "type": "object",
  "additionalProperties": false,
  "required": ["invocation_id", "role", "capabilities"],
  "properties": {
    "invocation_id": { "type": "string" },
    "role": { "enum": ["planner", "executor", "verifier", "reviewer"] },
    "capabilities": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["name", "purpose", "path", "load_trigger", "selected"],
        "properties": {
          "name": { "type": "string" },
          "purpose": { "type": "string", "description": "One line. Why this packet might need it." },
          "path": { "type": "string" },
          "load_trigger": { "type": "string", "description": "The condition under which the worker should read it." },
          "selected": { "type": "boolean" },
          "reason_not_selected": { "type": "string" }
        }
      }
    },
    "catalog_injected": {
      "const": false,
      "description": "Always false. A packet carrying a full skill catalog is a packaging defect."
    }
  }
}
