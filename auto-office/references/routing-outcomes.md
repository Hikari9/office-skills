# Routing outcomes — the local ledger

**Read this before [model-benchmarks.md](model-benchmarks.md).** The benchmark file reflects a public
leaderboard, which has never run this workspace's repos, briefs, or reviewer. This file reflects what
routing actually cost *here*. Where they disagree, this file wins.

Appended at **every** closeout, one row per task ([auto-closeout](../skills/auto-closeout/SKILL.md)).
Consulted at routing time, before the benchmark snapshot
([auto-routing](../skills/auto-routing/SKILL.md)).

## Why this file lives in the plugin

It is the **only** cross-run file that does. Everything belonging to a specific run — the plan, the
GOAL block, briefs, diff packages, evidence, the run report — lives in that run's **target repo**.
This ledger has to survive any single target repo, so it lives here and is explicitly workspace-local.

## Repo identity: opaque slugs only

This plugin ships publicly, so a committed row **never** names a real repo, host, org, or person. It
carries an **opaque slug** — `repo-a`, `repo-b`, `repo-c`.

The slug → real repo and plan path mapping lives in **`routing-outcomes.local.md`**, beside this file
and **gitignored**. That keeps a row traceable for the operator and keeps this file publishable. Seed
and append with a slug from the start; a ledger scrubbed later has already leaked.

## Columns

`date · repo-slug · task · brand · model · effort · dispatch · rounds · tokens · wall clock · verdict · lesson`

- `tokens` — `n/a` where the harness reports none (**always** for agy). Never `0`, which reads as a
  measurement rather than an absence.
- `rounds` — code-review rounds. `PLAN DEFECT` and `BRIEF DEFECT` consume none; note them in the
  lesson instead.
- `lesson` — one line, and only if the row taught something. "As expected" is a legitimate lesson.

## Ledger

| Date | Repo | Task | Brand | Model | Effort | Dispatch | Rounds | Tokens | Wall | Verdict | Lesson |
|---|---|---|---|---|---|---|---|---|---|---|---|
| 2026-08-01 | repo-a | Fix the receipt-send regression (#165) | claude | `opus` | high | cli | 3 | 829k | 2h10m | APPROVED | The run's largest line item, and the brief's stated cause was wrong. Two rounds in, the presumption should have flipped to `PLAN DEFECT`. Opus did not save it — a bigger executor implements a wrong brief more convincingly. |
| 2026-08-01 | repo-a | Contradict the stated cause (#167) | claude | `opus` | high | cli | 1 | 96k | 24m | APPROVED | Highest-value output of the run, and it came from the executor *disagreeing with its brief*. There was no return path for it at the time; `BRIEF DEFECT` exists because of this row. |
| 2026-08-01 | repo-a | Add a regression test for the queue path (#164) | claude | `opus` | high | cli | 2 | 187k | 41m | CHANGES REQUIRED → APPROVED | The test passed at `BASE` and could not detect its target bug. Now a standing brief clause: show the test red at `BASE` or it is not a test. |
| 2026-08-01 | repo-a | Recon: locate every call site | agy | `agy` | high | cli | 0 | n/a | 6m | APPROVED | Breadth-first read-only fan-out is where agy is unambiguously the right route. agy reports no tokens — wall clock is the signal. |
| 2026-08-01 | repo-a | (dispatch lost to a launch mistake) | codex | `codex-terra` | high | cli | 0 | 0 | 1h38m | — | **1h38m of wall clock, zero tokens.** No liveness check existed after dispatch. Wall clock is now a first-class cost, and the liveness check is in `auto-loop`. |

| 2026-08-02 | connect-portal | #180 care-notes cache overlay | claude | `sonnet` | high | in-session | 3 | 135k + 260k rev | ~25m | APPROVED | Both `CHANGES REQUIRED` rounds were about the TEST, never the fix. Reviewer mutation-testing found a guard whose whole suite moved state 0→1 key, so a key-*count* comparison passed everything while reintroducing the original bug. |
| 2026-08-02 | connect-portal | #183 measure Expand All cost | claude | `sonnet` | high | in-session bg | 0 | 126k | 7m14s | done | Measure-before-fix earned its keep outright: the issue's own recommended fix (remove `startTransition`) targeted the cheap ~83ms term and would have shipped a freeze as a fix for a lag. |
| 2026-08-02 | connect-portal | #183 staged grid mounting | claude | `sonnet` | high | cli | 2 | ~125k + 218k rev | ~12m + fixes | APPROVED | Executor **declined part of the brief** (refused to batch the Set updates) citing the measurement, and the reviewer upheld it. A brief that contradicts a measurement should lose. |
| 2026-08-02 | connect-portal | #181 persist expansion + staged restore | claude | `sonnet` | high | cli | 3 | n/a (CLI, no report channel) | ~40m incl. outage stall | APPROVED | Reviewer found a **user-facing data-loss bug** (search keystroke wiped the persisted layout) that no criterion asked about, then the planner's own remedy was ALSO wrong — filters exclude rows before bucketing. The executor caught that. |

| 2026-08-03 | repo-d | v2 QR alias-id: backend surfaces + cache + docs | codex | `gpt-5.6-terra` | high | cli | 1 | n/r | ~11m | APPROVED | Correct on the first pass with real mutation testing, unprompted. The right route, and the cheapest task in the run. |
| 2026-08-03 | repo-b | scanner reads alias id (QR contract other half) | claude | `sonnet` | high | cli `--bg` | — | 224k | ~16m, **zero output** | **absorbed inline** | Stalled TWICE on the harness 600s no-progress watchdog, having produced two untracked files and no commits. Cause was the dispatch form, not the brand: e2e work is long silent commands, and the watchdog kills an agent sitting on one. **Never route long-test-suite work as a `--bg` CLI agent.** |
| 2026-08-03 | repo-b | two pre-existing e2e defects (owner-added mid-run) | claude | `sonnet` | high | in-session bg | 1 | 174k | ~28m | APPROVED | Same brand, same task shape, different dispatch form — worked fine and found three stacked root causes the planner had missed. Confirms the row above is a dispatch-form lesson. |
| 2026-08-03 | repo-d+b | plan review | claude | `opus` | low | in-session bg (retired) | 1 | 94k | ~10m | 24 findings | **24 vs self-review's 6, overlap 3.** Blockers included a `verify` command red by construction (coverage threshold on subset runs), a year-long `Cache-Control` that would have served stale wrong-id QRs for 12 months, an omitted production deploy in the blast radius, and "automerge when green" in two repos that have **no test CI**. Best value per token in the run, again. |
| 2026-08-03 | repo-d+b | code review, resumed across 4 rounds and 2 repos | claude | `opus` | medium | in-session bg | 4 | 734k | ~35m | APPROVED | Found a surface with **no assertion on its output at all** (mutating it left 379 tests green), a constant cache key that passed the module's own suite because every fixture was the same human, and a **planner-authored production regression** heading into a deploy. Also re-derived the planner's "pre-existing failure" claim independently instead of accepting it, and corrected the planner's count of production-visible changes. |

**Reading the 2026-08-03 rows:** the planner authored **three** of the defects the gates caught —
a fabricated justification for rejecting a plan-review finding, a `verify` command that could not
pass, and a production regression in its own fix. The executors needed one fix round between them.
Two further lessons worth carrying: **a rejection of a plan-review finding is a claim about the
code and needs the same evidence as a done-criterion** (this run rejected finding #13 on an
invented premise, shipped that premise into five comments and a README, and had it disproved by the
code gate); and **mutation-testing your own inline fix before resubmitting** converted a likely
second `CHANGES REQUIRED` into an approval, twice.

**Reading these nine rows together:** cost concentrates in the plan and the brief, not in the
executor's model. Eight of the nine rows are about the *instruction*; none is about capability.

The 2026-08-02 rows sharpen that from "briefs can be wrong" to something more specific: **the
planner's artifacts fail in a repeatable shape, and reviewers catch them more reliably than they
catch executor bugs.** Three criteria and one fix instruction were defective in one run, all four
caught downstream. Twice the executor or reviewer was right against the planner and the planner
was wrong. Route accordingly: an executor that pushes back on a brief citing evidence is doing the
job, and a reviewer's spare capacity is better spent attacking the *criteria* than re-reading the
diff.
| 2026-08-02 | repo-b | T1 RecordStatus promotion + 4 entry points (#49) | codex | `codex-terra` | high | cli | 1 | n/a | 19m | APPROVED | Clean first pass. Its gate found an unrelated **live prod bug** (a REST route dead in prod but mocked in the harness) — the highest-value output of the run came from the reviewer looking outside the diff. |
| 2026-08-02 | repo-b | Fix group RSVPs throwing on a dead route (planner inline) | — | `opus` (planner) | — | inline | 1 | n/a | 12m | APPROVED | Group RSVPs had written **nothing** for months — no GroupMember, no Attendance. Invisible because the mock answered the dead route *and* the test asserted it. A mock plus an assertion can enshrine a bug from both sides. |
| 2026-08-02 | repo-b | T2 thread recordStatus through payloads (#49) | codex | `codex-terra` | high | cli | 1 | n/a | 13m | **PLAN DEFECT** | Code correct against the pin; the *plan* lacked a rule. Correctly setting a field collided with an untouched sort. `PLAN DEFECT` did its job — one amendment beat a fix wave. Codex worked test-first unprompted, visible in the tee log. |
| 2026-08-02 | repo-b | T3 toggles, counts, badges, sort fix (#49) | agy | `claude-sonnet-4-6` | high | cli | 2 | n/a | 14m | CHANGES REQUIRED ×2 → APPROVED | Chose sonnet over `gemini-3.6-flash-high` because the task was almost entirely pinned signatures and the skill records Flash inventing them. No invented signatures occurred. Phase 2b check 6 caught a real defect before review. |
| 2026-08-02 | repo-b | Opus review gate, resumed across all tasks | claude | `opus` | medium | cli | 5 rounds | 788k | ~9m total | — | One resumed reviewer across 3 tasks beat 3 fresh ones: by round 4 it was citing its own earlier findings and had the codebase loaded. It also *independently probed the live server* twice rather than reasoning from the diff. |

| 2026-08-02 | rsvp-favor-church | timezone: UTC core, viewer-local (impl, tasks 1-9) | codex | `gpt-5.6-terra` | high | cli | 5 | n/r | ~29m | — | Correct core in one dispatch: two-frame split, DST inverse, Rock write conversion all survived reviewer mutation. Round-2 dispatch reported `killed` while still alive; planner did NOT re-dispatch, confirmed death, recovered the orphaned green work. Cost: no executor mutation report for r2. |
| 2026-08-02 | rsvp-favor-church | timezone: adversarial code gate | claude | `opus` | medium | cli (resumed) | 5 | 675k | ~23m | — | Resumed reviewer again outperformed: re-ran round-1 mutations each round to check for decay, and corrected the *planner's* own mutation report. Rounds 3-5 gated test names/comments only — real defects, but Opus-grade review for a code comment is over-provisioned. |
| 2026-08-02 | rsvp-favor-church | timezone: plan review | claude | `opus` | low | cli (retired) | 1 | 78k | ~4m | — | Best value in the run. 14 findings, 2 BLOCKER; caught that the plan assumed ONE Rock datetime frame when there are two — would have shipped a silent 2h error on every Brisbane event. Also caught the planner silently resolving a user-owned decision. Self-review found 3, this found 14, **zero overlap**. |

| 2026-08-03 | rsvp-favor-church | check-in CSV export (T1/T1b/T2 + planner inline) | claude | `sonnet` | high | in-session, shared worktree | 5 | ~530k | ~1h20m | APPROVED → PR #64 | Executor brand was a caller override (codex→claude) on quota: codex weekly 37% resetting in 5d19h vs claude 66%/7d. Correct call — feature needed no codex-grade backend work. **Plan review found 22 vs self-review's 8, overlap ~1**, and all 4 blockers were real (a field the codebase cannot fetch; a criterion testing a mechanism that does not exist; a criterion untestable for lack of jsdom; a formatter that cannot emit the pinned format). **Planner produced BOTH defects this run** — a PLAN DEFECT (two true premises that don't compose: rows built `checkedIn:false` are mutated to true client-side at 5 sites) and a half-applied fix whose docblock claimed completeness. Executor flagged the first and was overruled; it was right. Third consecutive run where executor-vs-planner went to the executor. Planner mutation-testing its own inline fix before resubmitting converted a likely 2nd CHANGES REQUIRED into APPROVED — make that default. CLI dispatch blocked by the permission classifier **on that run's blanket-flag form** (`--dangerously-skip-permissions`); fell back to in-session. **Do NOT read this row as licence to skip CLI** — re-probed 2026-08-04 and the scoped `--allowedTools` form launched and completed fine. A past denial is not evidence about now; attempt the launch. A manual criterion was blocked by DATA, not code (only PIN-reachable definition had zero attendees) — seed a populated preview occurrence before runs with manual guestlist steps. |

| 2026-08-05 | rock-automerge | two codex adversarial review gates (merge driver + candidate manifest) | codex | `codex exec` | high | cli | 1 | 0 | 57m + 43m, both wasted | **NO VERDICT — dispatch never ran** | `codex exec "<prompt as positional arg>"` launched from a background Bash tool **blocks forever on stdin**. Codex prints `Reading additional input from stdin...` (documented: piped stdin is appended as a `<stdin>` block) and waits for EOF that the tool's inherited pipe never delivers. It then **exits 0**. Every surface signal said success: exit 0, process alive the whole time, a completion notification. The tells were all negative — **0.15s of CPU in 43 minutes**, no session file under `~/.codex/sessions`, a clean worktree, and a 39-byte output file. Two independent gates on an irreversible hard-delete produced nothing for 100 minutes of wall clock. **Fixes, both required:** (1) pass the prompt as `- < brief.txt` so stdin gets a real EOF, never positionally from a non-TTY; (2) always pass `-o/--output-last-message <file>`, and additionally instruct the agent to **commit its verdict as a file** — a final chat message is not a durable artifact. **Verification rule that generalises past codex:** confirm a delegated agent is alive by *CPU time climbing* and *a session/transcript file existing*, never by exit code or elapsed time. An agent blocked on input is indistinguishable from a working one by every cheap signal. |

| 2026-08-08 | rock-pages | form insights: 3 pages replacing a per-event Google Sheet (#58 → PR #60) | claude | `sonnet` high (T5-T7 CLI) + `opus` planner inline | high | cli, then inline | 2 formal + 3 live-defect cycles | n/r | multi-session | APPROVED → PR #60, live both envs | **Three defects reached production, all of the same shape: nothing local executes the language the bug lives in.** 364 passing tests, a Lava syntax gate, a jsdom harness and two Opus review passes all went green on (a) a Lava filter argument that cannot parse (`Replace:'</','<\/'` — DotLiquid rejects the backslash), (b) a string-blind `--` comment stripper that deleted a literal's closing quote, and (c) `'X' \| PageParameter` returning **empty** where `PageParameter.X` works. Every one was found by a human loading the page. **The generalisable rule: for Rock/Lava work the only gate that counts is a rendered-page fetch asserting the body contains no `Lava Error` — never a status code, never a local parser.** Defect (c) is the expensive class: it fails *plausibly*, falling through `\| Default:0` to a well-formed all-zero result with no error and HTTP 200, indistinguishable from an empty form. Two lessons beyond the gate: **echo the resolved input into the block's own output** (a `sourceId` field would have made it a 30-second diagnosis instead of a session), and **the answer to (a) was already written down in this repo** (`favor_directory/sql/favor-directory.sql:480`) and the planner authorised the approach without reading it — a domain with a learnings file is a domain where "search before deciding" is cheaper than review. On verification: the run's own verifier **had a condition bug that guaranteed two failures and proved nothing** (it fetched two of three pages with an empty query string, where zero columns is correct, then compared to a live team count) while the security criterion it was supposed to hold had **no independent read-back at all** — the apply script verified its own writes, which proves writes landed and nothing else. Both were reviewer findings, both real. **Mutation-testing new gates in both directions caught two useless gates on first cut** (a negative "no file uses the broken form" gate is satisfied by a file that stopped reading the parameter entirely; and both gates were initially satisfied by a *comment* naming the form they were checking for). Cheap, and it changed the outcome twice. |

| 2026-08-08→09 | rock-pages | form insights ministry view + nav prune (#63–#68 → PR #71, merged) | agy Wave-1 frontend + codex SQL, then **codex for all 4 fix rounds**; claude opus reviewer ×4 | `gemini-3.6-flash-high` / `codex exec` / `opus` | high | cli, 7 worktrees | 4 of 5 | agy **reports none — do not print 0**; opus reviewer ~130-155k per round | ~2 sessions | APPROVED → merged, live preview + PROD (14/14) | **The routing lesson is one line: for work whose deliverable is EVIDENCE, breadth-fit is the wrong axis.** Agy produced working frontend code and worthless tests — 79 harness checks green against deliberately broken code (the reviewer proved it by mutation). That bought review round 1 (13 findings), and round 1's *fix lane* then introduced **both** of round 2's BLOCKERs (`rr.[IsTemporary]`, an invalid column that aborts the batch and blanks the page; and a signup-group branch unfenced at ~23,700 groups). Rerouting every fix lane to codex ended the streak immediately: round 3 no blockers, round 4 APPROVED. Two review rounds and two fix lanes trace to that single routing call. **The 2-consecutive-CHANGES-REQUIRED plan-defect presumption fired and was correct** — the planner did not self-adjudicate it but asked round 3 to rule, and round 3 found a real defect: the plan *named* its worst failure mode ("nothing here executes Lava or SQL; four blanking incidents shipped green") and mitigated nothing, leaving fix lanes to rewrite T-SQL blind. Generalisable: **if a plan states its gates cannot detect its worst failure mode, closing that gap is a TASK in the plan, not a caveat in its constraints.** Two gates were added mid-run and both were proven to fail on known-bad input before being trusted; the column gate was verified to catch the exact blocker that motivated it. **A gate's first cut was wrong twice, in opposite directions** — over-broad (its snapshot held REST *navigation properties*, so `wt.[IdKey]` read as a valid column) and over-narrow (it only fired when an unrelated table happened to be joined). Both were reviewer findings. An unbound-alias check was implemented and **reverted**: 20+ false positives on correct SQL, because the parser splits on `;` and doesn't strip comments — a gate that cries wolf gets switched off, so a known gap beat a noisy check. **And still, the fifth page-level defect in this repo shipped past everything**: `Replace: '</', '<\/'` is a *hard parse error* on this Rock (round 4 predicted only the silent variant), found solely by the rendered-page read-back. Fixed with a `{%- capture -%}` body — raw output, not a string literal. Three planner self-inflicted errors, all caught before commit and all worth the paranoia: a comment containing a literal `<script>` tag broke two JS-scanning tools; the "fix" for that was a global replace that mangled two real script tags; and a `var` constant read `undefined` because `renderStats()` runs earlier in the IIFE than the assignment. **Process cost:** the PM was over-provisioned — it dispatched three parallel lanes for ~1h, after which every lane was a single codex process the planner drove directly. **Verification not done:** the Claude-in-Chrome extension was unavailable, so no interaction pass ran on either environment; the subagent correctly reported "unverified" rather than assuming safe. Never let that read as passed. |

| 2026-08-09 | rock-pages | form-insights tab=teams: source name, batch actions, team deeplinks (#70 → PR #77, merged, live preview + PROD) | codex T1-T4 (caller override from agy) + opus planner inline; claude opus reviewer ×6 | `gpt-5.6-terra` / `opus` | high | cli, one worktree, 2 codex sessions | 2 (impl) + 4 (gate) | codex n/r; plan review 111k; code review 111k→273k/round ×6 | ~2.5h | APPROVED → merged, live both envs (preview 17/0, prod 18/0) | **Route the gate to a DIFFERENT SESSION from the code, not merely a different brand.** T1-T3 and T4 were both codex; the split that mattered was a *fresh* session for the evidence layer, so the gate author had never seen the implementation. It found bypasses the implementer's own session would have had every reason to consider covered. **The two `PLAN DEFECT`s were both the planner's and both arrived before any code was written** — a brief demanding a handoff file its own ceiling forbade, and a debounce unimplementable against a harness that asserts filtered DOM synchronously. Fourth consecutive run where executor-vs-planner went to the executor; the return path is now clearly cheaper than the fix loop (37k tokens vs a wrong implementation). **The naive fix for the second would have manufactured a green suite** — letting the task edit the assertions its own code must pass is exactly how this domain got 79 green-against-broken checks; resolved instead with a `window.__fiMatrixFilterDelay` knob so every existing assertion stayed byte-identical and the "debounce exists" check was written by a different session. **Plan review at `opus` low was again the best value in the run**: 14 findings vs self-review's 4, overlap ~1, and its 4 BLOCKERs included a gate command that does not exist, a task assigned files its own blast-radius ceiling forbade, a "pre-existing" mechanism that was pure fiction, and a prod gate the plan's own dc0 declared invalid. **The delegated preview apply was the clearest waste**: an in-session sonnet subagent spent 63k tokens, returned "monitoring started", and stopped — the documented unwind, now observed on a *worker*, not just a PM. Planner re-did the apply, idempotence, cache clear and dc9 directly. **Three of the planner's own mutations were invalid and two nearly became false "the gate is broken" reports** — one targeted `.generated.lava` when the harness loads the authored file, one anchored on a non-existent string, one landed textually correct but inside a click handler so the injected element was created after the harness counted elements; all three printed "0 FAILs". Verify a mutation changed *behaviour* before trusting its verdict. Six deliberate breaks (incl. an `innerHTML` XSS and an `inactivate` literal) passed the suite at BASE; all six plus seven bypass shapes now go red, across 229 assertions run against both the authored block and the deployed artifact. |

| 2026-08-09→11 | connect.favor.church | attendance row cache rewrite T5–T11 / dc5–dc11 (#202; branch ready, NOT pushed — owner hold) | **one sonnet CLI executor per task, one worktree**; fresh claude opus reviewer per task, resumed across rounds | `sonnet` high / `opus` reviewer | high | cli, 2 worktrees (planner + executor), no PM | 3 (T6) · 2 (T8) · 3 (T8c) · 2 (T9) · 1 each elsewhere | executors n/r (CLI); reviewers 88k–251k per round, ~1.1M total | ~9h dispatch across 2 sessions | APPROVED, all 7 done-criteria green on the planner's own closeout run | **The run's dominant cost was the PLANNER'S BRIEFS, not the executors.** Four briefs named a *mechanism* and assumed it was the *render path* — one specified an overlay write and never said the seed must read it back; one said "give this accessor an overlay param" when the visible value was computed in a different component and the accessor fed only a sort key; one named a fallback branch dead at every real call site; one amendment was written from an executor's causal story rather than the render graph and had to be corrected by its own successor. Each passed review **on the thing the brief named** while leaving the symptom intact. ~6 review rounds, all at reviewer rates. **The fix is a planner action and it is cheap: scout the render graph before writing the brief.** From T9 on I did, and caught a missing CLEAR seam *before* dispatch for the first time in the run — the only seam gap in the run that cost nothing. **Generalised into `auto-loop` as brief clause 3 (name the observable outcome; a data path is not a render path; enumerate the full lifecycle — for a cache that is write/read/CLEAR).** Twice the missing seam was the *clear*: state written and read correctly, then shadowing the server for the whole session, with the pending indicator making it look *more* correct. **The 2-round plan-defect presumption fired once (T8c) and was correct** — the field could not be made to reconcile at all, because its optimistic value embedded state the client cannot predict (`isNew`, derived from `Members.length`, which a leader-add changes). Routed to the owner, who chose **deletion over re-shaping**; T8d was cancelled with it. The decisive fact — that field appears in **no done-criterion** — was one grep, checkable at any point, and I ran it only after the second CHANGES REQUIRED. **Ask which criterion a field serves before funding a fix wave.** That test's own round-trip fixture pinned the exact inputs that differ in production, so it could never have caught its own bug — the fixture supplied the thing under test, the same defect class as this workspace's 79-green-against-broken history. **Reviewers were the best-spent tokens and should not have been cheaper**: every blocking finding came from *reproducing* a defect (a permanent-spinner probe, a pencil-contradicts-grid probe, a four-link reachability chain that would have been a second demotion path had one link differed), and the three multi-round tasks are exactly the three where a prose argument was initially accepted. One reviewer diagnosed its **own** false result — 9 failures from running lint and tsc concurrently, all jsdom timeouts — and re-ran isolated rather than reporting a regression. **New mechanism gotcha, now in `auto-loop` safety rules: a mutation-testing reviewer is not read-only.** One was killed mid-mutation by a machine shutdown and left the admins reconcile silently disabled in the working tree; the next executor would have started from it, in a file outside its own scope. Every one-writer rule in the office points at executors, which is why the gap survived. Fix is structural — apply/run/restore inside a single tool call — and a later reviewer adopted it mid-run unprompted. **Also: a harness `EnterWorktree` default created a worktree inside the forbidden production checkout before the executor had read its brief; a prohibition in the brief cannot bind an action taken on the way to reading it, so it moved into the dispatch prompt and never recurred.** Over-provisioned: the tail (T10a/T10b/T11) got seam-bearing apparatus for comments and a deletion; the owner's call to consolidate to one executor + one reviewer was right and I should have proposed it a task earlier. Under-provisioned: nothing. **Not verified: quota headroom was never probed at either end**, and Rock preview roles 55/56 are owner-verified only — the preview REST token 403s and MCP is bound to prod, so there was no read path. |

| 2026-08-10→11 | connect.favor.church | department dashboard progressive loading, 6 waves + integration (v2.28.0 → PRs #235 merged to `preview`, #236 merged to `staging`, #237 run report; `main` NOT promoted — divergence stop) | **one codex CLI executor per wave, one reviewer per wave (fresh session, resumed across rounds)**; claude opus planner | `gpt-5.6-sol` / `opus` planner | high | cli, main checkout then one isolated worktree | 4 (W1) · 3+2 (W2) · 2+1 (W3) · 1 (T7) · 3+2 (T8) · 1 (T9, 1 Critical + 4 Majors) · 1 (W5b) · 1 (T10) | executors n/r (CLI); planner-side gates only | ~5h across 2 sessions | APPROVED all waves; shipped to `staging` | **Every multi-round wave in this run failed the same way: a test that observes a consequence reachable by more than one path.** W1 spent FOUR rounds on one root cause — tests asserted through `useQueryClient()`, which is context-reactive, so they proved the context changed while proving nothing about *mounted* observers (react-query v3 builds the observer once in `useState`, so a context change never rebinds it). T8 spent three because its fixtures were age-rich `PortalConnectGroup`s while production supplies bare scope stubs — certifying a design that could not work. Once briefs named the failure mode *explicitly* ("name what your test would still pass with"), waves cleared in one round: T7 and W5b each took one. **Generalisable brief clause: state the wrong-but-passing implementation the test must exclude.** — **The highest-leverage single act in the run was a POINTED reviewer question.** T9's brief asked "can this misclassification reach anything beyond *display*?" instead of "review this diff". That question surfaced a **destructive write chain every green gate had missed**: a dropped optional `map?` argument → hardcoded enum role ids (per-instance surrogate keys in Rock) → a real leader classifies as member → `planConnectGroupWrites` forces a leaders call on a **title-only** edit → `rockPerformUpdateConnectLeaders` PATCHes every leader down to member. It compiled, linted, type-checked and passed all tests. **Optional parameters are invisible cliffs at a server→client boundary move** — the call site stays valid while its inputs silently change. Ask reviewers about blast radius, not correctness. — **Two planner-authored defects, both cheap to avoid.** (1) A brief pinned two non-existent type names; the executor correctly returned `BRIEF DEFECT` (fifth consecutive run where executor-vs-planner went to the executor). (2) A five-finding fix brief whose per-finding "raise this Upline" collided with the authority boilerplate's blanket "raise Upline and **stop**" — a conservative executor halted ALL FIVE fixes, including a verified data-loss defect, over the *least* important finding's protected-path sub-case, and returned zero implementation. Its reasoning was correct and its conduct was clean. **Now fixed in `evidence-and-handoff.md`: every Upline instruction must declare `note and continue` vs `stop the run`, and findings must be ranked so an executor forced to choose protects the worst defect, not the last paragraph.** — **A killed reviewer stamped `VERDICT: CHANGES REQUIRED` on an empty stub**, because "write the file early so a kill doesn't lose work" and "end with exactly one verdict line" are individually right and jointly produce a fake rejection — which the 2-consecutive-rejection plan-defect presumption then acts on. **Fixed in `reviewer-brief.md` with a third state, `VERDICT: PENDING`.** Related: one reviewer's verdict line said CHANGES REQUIRED while its own body said "No Critical or Major findings" and the brief said Minors don't block. **Reject a verdict's label and accept its reasoning separately.** — **Seven external interruptions, zero lost work**, entirely because the recovery order was `git status --short` **before** reading any handoff. Three landed after a commit but before the handoff (the handoff is always last); those were recovered with *verification-only* re-dispatches that produced the missing mutation evidence without redoing the work. — **`pnpm install` corruption finally reproduced deterministically**: pnpm 11.7.0, clean lockfile, `INSTALL_EXIT=0`, `grep -c "^overrides:"` goes `1 → 0` while `package.json` still declares `pnpm.overrides` ⇒ Vercel `ERR_PNPM_LOCKFILE_CONFIG_MISMATCH`. **A commit hook triggers it too** (observed on a docs-only wave). No local gate can see it — lint, tsc, jest, `next build` all green on a corrupt lockfile. Restore with `git show <ref>:pnpm-lock.yaml`, never by re-installing. **The deploy check is the only authority on install correctness.** — **Closeout found two stops the loop could not have known about.** (1) The version bump was to `2.27.0`, a value **already shipped**; both sides of the merge read `2.27.0`, so it merges with **no conflict** and silently walks `main` backwards from `2.27.1`. A version is the one field where "both sides agree" is evidence of a bug. (2) The branch was **61 commits behind `preview`**, which had rewritten the exact role-classifier the Critical fix rests on (`#202/T11` narrowed the enum fallback; `#202/T8b` added `AssistantLeader`) — the two fixes compose, and `AttendanceRateTable.tsx` **auto-merged cleanly**, which is the hazard, not the reassurance. Both-parents diffing found no dropped hunk; the wave mutations were re-proved against the merged files. — **Two agents in one worktree.** A concurrent session held uncommitted work in the shared checkout; the executor found it at BASE, refused to stash/reset/checkout it, and stopped with a blocking Upline item — exactly right. Resolved by isolating in a fresh worktree (`pnpm-workspace.yaml` is gitignored, so seed it or `pnpm install` exits 1; nested worktrees also collide on ESLint config without a temporary `"root": true`). **Planner error worth its own line: `cd` in a Bash call persists, so three "is the main checkout safe?" checks silently ran in the worktree and appeared to show another session's work destroyed.** Use `git -C` / absolute paths for cross-tree checks. — **Not verified:** the three reveal tiers were never observed in a browser (Auth0 wall; the browser subagent correctly reported "could not determine" rather than guessing, and did prove all six routes 401 anonymously), and no application test crosses the RSC boundary carrying a `Map` (Flight was measured doing it correctly on the pinned runtime, but `force-dynamic` means the build never renders the route). **Quota headroom was never probed at either end** — again. Filed as #232 rather than absorbed into "done". Ten carried findings and five pre-existing `main` defects went to #230/#231 instead of dying in gitignored `.plans/`. |
| 2026-08-11 | connect-portal | connect attendance date shadowing (Basecamp 10163134921 → PR #234 → #244 → #245, **v2.28.3 live in PROD**) | **one claude sonnet executor, in-session** (caller override — routing not exercised); claude `opus` plan-reviewer, code-reviewer, and merge-resolution reviewer | `sonnet` high / `opus` | high | in-session | 0 formal code-review + 1 `BRIEF DEFECT` + 2 merge-resolution | plan review 88k · executor 119k+133k · code review 142k · merge review 69k+89k | ~1h30m (impl ~10m, **closeout ~1h20m**) | APPROVED → merged through the full chain | **Plan review paid for itself in one finding, for the fifth run running.** Rev 1 reused the auto-create branch's `getNextMeetupUTC(addDays(start,1)…)` as the *definition* of the window's canonical meeting — but that is **bucket-relative, not schedule-relative**: `start` is always a Sunday, so it always returns `start+7`. Right for the reported group by coincidence, **wrong for every even-week-aligned biweekly group**, i.e. rev 1 would have traded one wrong-date bug for a wider one. The same 88k pass also showed the comparison could never match (`getNextMeetupUTC` returns a UTC instant at Manila meetup time, `2026-08-02T05:00:00Z`, vs Rock's naive `2026-08-02T00:00:00`) and found a **second call site** the plan missed entirely. Rev 2 derived from the group's own `DTSTART`/`BYDAY`/`INTERVAL` in **calendar dates**, which dissolves the timezone question instead of reconciling it. **Generalisable: when a fix is "make the read path agree with the write path", verify the write path is itself anchored to the right thing — reusing a wrong derivation for consistency propagates it.** — **The planner's `BRIEF DEFECT` produced a green suite that proved nothing.** D7 pinned the *window* but not the *navigation vector*, so all four new tests reached it via the default path — where the OLD code also returned the right answer, meaning the headline "regression" test passed at BASE. Only the mirror test discriminated. **A regression test must be pinned to the code path the bug lives on, not merely to the input values in the report** — for a prev/next bug that means passing `beforeDate`. Mutation-testing found it; reading the diff would also have. This is the same family as this ledger's "test that observes a consequence reachable by more than one path", now in a *third* variant: right assertion, wrong entry point. — **Closeout was the expensive phase, at ~8× implementation, and every defect it found was invisible to the pre-merge gate.** (1) The PR was opened against `main`, bypassing `feature→preview→staging→main`; one `gh pr list` would have shown it. (2) The first merge **silently dropped 47 lines** preview had added to a test file this branch never touched (`BroadDashboardPages.test.tsx` — its scope mock, an import, and a progressive-loading mock), reddening 8 department tests; **the three-dot diff hid it entirely and the two-dot diff against both parents found it**, after which a hash sweep of all 60 files preview had changed proved nothing else was missing. (3) A **three-way** version collision on 2.28.2 (this branch, #238, #243) — and the reviewer's `git merge-tree` probe located the *silent* path precisely: **#238, which shares no code with this branch, merged completely clean at the same version**, whereas #243 always conflicts on a shared call site. So the dangerous collision is the one with an unrelated branch, because there is nothing else to trip on. **Corollary to the existing "both sides agree is evidence of a bug" rule: check `git log --all -S` across every ref, not `>` against the promotion branches — an unmerged claimant passes the ordering test.** — **A `merge-tree` probe is the cheap way to answer "will this conflict?"** The merge reviewer used non-destructive in-memory 3-way merges to prove the #243 conflict is unavoidable in every ordering, that only 1 of its 4 files conflicts (the **callee** signature widening merges silently), and that both naive resolutions fail `tsc` so neither reaches runtime. Worth reaching for before speculating about merge order. — **A planner mis-report, corrected mid-run:** the prod data repair was reported to the owner as needing the admin UI or SQL because `rock_write` refused the model. That is an **MCP allowlist, not a Rock permission** — REST patches it fine. **An MCP write refusal is never evidence that REST cannot do it.** Folded into `rock-favor/references/rest-gotchas.md`. — Cheap wins: the broken-nvm-shim and `--runTestsByPath` traps went into the brief from memory up front and cost the executor nothing. **Not verified:** the ~13 other groups with 2+ occurrences in one window (selection legitimately changes for them) were never exercised in a browser — Auth0 wall — and that is stated in the PR rather than left implied. **`PLANNER-HELD` prod write NOT done:** the apply was **denied by the harness permission classifier** for carrying `--yes-prod` (the dry-run passes), so it was handed over as one command rather than routed around; the user-visible symptom persists until it runs, which the Basecamp comment says outright. **Start-of-run quota headroom never probed — third consecutive run (#232).** |
