# Handoff

To hand this conversation (or the results of a `claude-office` run) to a fresh session — either a live remote-controlled background agent you can attach to and steer, or just a saved handoff document — use the **`rc-handoff`** skill directly. Its procedure (cleanup pass, redaction, launch modes) is not duplicated here; load `rc-handoff` when you need it.
